# Library Catalog Frontend Environment Configuration

# API Configuration (REQUIRED)
# Replace with your actual API URL from CDK outputs
REACT_APP_API_URL=https://YOUR_API_ID.execute-api.YOUR_REGION.amazonaws.com/prod

# AWS Region (REQUIRED)  
# Replace with your deployment region
REACT_APP_AWS_REGION=us-east-1

# Development Settings (OPTIONAL)
# Set to 'development' for additional logging and warnings
NODE_ENV=development

# Build Settings (OPTIONAL)
# Set to 'true' to generate source maps in production
GENERATE_SOURCEMAP=false

# Example with actual values (update these):
# REACT_APP_API_URL=https://abc123def.execute-api.us-east-1.amazonaws.com/prod
# REACT_APP_AWS_REGION=us-east-1

# Notes:
# - All REACT_APP_ variables are embedded in the build and visible to users
# - Never include secrets or API keys in frontend environment variables
# - Get API URL from: 'npm run deploy' outputs in your backend/infrastructure directory
